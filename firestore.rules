rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Relaxing permissions as requested to move exclusively to Supabase for Auth.
      // This allows the app to function with Firestore data while using Supabase Auth.
      allow read, write: if true;
    }
  }
}
